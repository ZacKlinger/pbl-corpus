# Sources wishlist
#
# Format: one entry per line.
#   <filename> <url>
# Filename must not contain slashes. Lines starting with # are ignored.
#
# The fetch-sources GitHub Action reads this file, downloads each URL into
# sources-raw/, runs a MIME-type check (a .pdf line that returns text/html
# is recorded as WRONG-TYPE and the file is deleted), and commits the bytes
# back to main.
#
# Agent triage on 2026-05-28 (first weekly run): the two priority anchors
# (Condliffe 2017 and HQPBL 2018) fetched successfully and have been
# verified into sources/. The five entries that failed on the first fetch
# run have been demoted to leads/ with access-status notes. The wishlist
# below is empty pending mirror URLs from Zack — see the digest's "Where I
# need you" section.

# --- Successfully fetched and verified into sources/ (kept for reference) ---
# Condliffe-2017-pbl-literature-review.pdf https://www.mdrc.org/sites/default/files/Project-Based_Learning-LitRev_Final.pdf
# HQPBL-2018-framework.pdf https://hqpbl.org/wp-content/uploads/2018/03/FrameworkforHQPBL.pdf

# --- Demoted to leads/ on 2026-05-28 (mirror URLs needed) ---
# See leads/PBLWorks-2015-gold-standard-pbl.md
# (Original wishlist attempt: my.pblworks.org → WRONG-TYPE, likely auth wall)
#
# See leads/Kilpatrick-1918-project-method.md
# (Original wishlist attempt: educacao.cefetmg.br → curl 6 DNS failure;
#  the text is public domain and mirrored on archive.org if a stable
#  direct-PDF URL can be confirmed)
#
# See leads/Chen-Yang-2019-pbl-meta-analysis.md
# (Original wishlist attempt: sciencedirect.com → curl 22, Elsevier paywall;
#  needs institutional access or author preprint)
#
# See leads/Saavedra-2021-knowledge-in-action-ap-gov.md
# (Original wishlist attempt: lucasedresearch.org → curl 22, URL no longer
#  resolves; the report is public — needs current URL)
#
# See leads/EL-Education-core-practices.md
# (Original wishlist attempt: eleducation.org → WRONG-TYPE, likely gated;
#  the Core Practices doc is publicly distributed — needs current URL)
